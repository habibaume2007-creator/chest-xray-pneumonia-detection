# Trained Models

The trained ResNet50 model weights generated during this research project are not uploaded directly due to GitHub file size limitations.

The models include:

- resnet50_pneumonia_final.pth
- resnet50_pneumonia_experiment1_best.pth

These files can be generated by running the training pipeline provided in the notebook.

